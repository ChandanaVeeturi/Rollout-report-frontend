# Copy this to .env.local for local dev (Vite proxy handles /api automatically, so leave blank)
# Set this in your deployment platform (Vercel / Netlify / Railway) for production
VITE_API_BASE_URL=https://rollout-report-backend-production.up.railway.app/api
